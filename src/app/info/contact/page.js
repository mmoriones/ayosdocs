import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact Us | AyosDocs Support',
  description: 'Get in touch with the AyosDocs team for support, feedback, or suggestions.',
};

export default function ContactPage() {
  return <ContactClient />;
}
